#ifndef ENCODER_H
#define ENCODER_H
#include <fstream>
#include <string>
#include "class/shan_table.class.h"

class Encoder
{
	private:
		ShanTable mShan;
		void writeOnBuffer(int a,char *outBuffer,int* blockIndex,int* bitSize);
		void writeTable();
	public:
		Encoder();
		void fileName(std::string fileName,std::string output);
		void startSample();		
		void startWriting();
		void close();
		void printTable();
		
};


#endif
